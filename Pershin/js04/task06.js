// Значения переменных X, Y, Z поменять местами так, 
// чтобы они оказались упорядоченными по возрастанию. 


let data = ['abc', 'fff', 'zzz']
let data2 = [1, 10, 8]

function sort(arr) {
    let sortedArr = []
    for(let i=0; i<arr.length; i++) {
        let item = arr[i]

        for(let j= i + 1; j<arr.length;j++) {

        }

    }

    // for(let item of arr) {
    //     item
    // }

    return sortedArr
}

console.log('отсортированный массив ' + sort(data))

